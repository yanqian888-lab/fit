# 服务器部署报告

> 部署时间：2026-06-18 18:16

---

## 服务器信息

| 项目 | 值 |
|------|-----|
| 公网 IP | 39.96.67.113 |
| 系统 | Ubuntu 22.04 LTS |
| 配置 | 2 vCPU / 2 GiB / 40 GiB ESSD |
| 地域 | 华北 2（北京）|
| 到期时间 | 2027-06-18 |

---

## 已安装环境

| 软件 | 版本 | 用途 |
|------|------|------|
| Node.js | v20.20.2 | 运行后端服务 |
| npm | 10.8.2 | 包管理 |
| PM2 | 7.0.1 | 进程管理与开机自启 |
| Nginx | 1.18.0 | 反向代理 |
| Git | 2.34.1 | 拉取代码 |
| SQLite | 内嵌 | 数据存储 |

---

## 项目部署位置

代码部署在服务器：
```
/opt/jianfeidazi
```

目录结构：
```
/opt/jianfeidazi/
├── backend/          # Node.js 后端
│   ├── src/          # 源代码
│   ├── data/         # SQLite 数据库
│   └── .env          # 环境变量
├── frontend/         # UniApp 前端
├── docs/             # 文档
└── word/             # 原始需求文档
```

---

## 服务状态

| 服务 | 状态 | 访问地址 |
|------|------|----------|
| Node.js 后端 | ✅ 运行中 | http://39.96.67.113:3000 |
| Nginx 反向代理 | ✅ 运行中 | http://39.96.67.113 |
| PM2 进程管理 | ✅ 已配置开机自启 | - |

---

## 接口验证

### 健康检查
```bash
curl http://39.96.67.113/api/health
```
响应：
```json
{"code":0,"message":"ok","data":{"time":"2026-06-18T10:16:35.865Z"}}
```

### 登录测试
```bash
curl -X POST http://39.96.67.113/api/auth/wechat-login \
  -H "Content-Type: application/json" \
  -d '{"code":"test"}'
```
响应：正常返回 token 和用户信息。

---

## 环境变量

配置文件位置：
```
/opt/jianfeidazi/backend/.env
```

当前配置：
```env
PORT=3000
JWT_SECRET=（已生成随机 64 位十六进制密钥）
DB_PATH=./data/app.db
```

⚠️ **还需要手动配置**（豆包 AI）：
```env
DOUBAO_API_KEY=你的豆包 API Key
DOUBAO_ENDPOINT_MAIN=ep-xxxxxxxxxx（主协调 Agent）
DOUBAO_ENDPOINT_PRECIPITATION=ep-xxxxxxxxxx（沉淀 Agent）
DOUBAO_ENDPOINT_HELPER=ep-xxxxxxxxxx（全能助手 Agent）
```

配置完成后重启：
```bash
pm2 restart jianfeidazi-backend
```

---

## 防火墙配置

已放行端口：

| 端口 | 用途 |
|------|------|
| 22 | SSH 远程连接 |
| 80 | HTTP / Nginx |
| 443 | HTTPS（后续 SSL）|
| 3000 | Node.js 后端（开发调试）|

---

## 常用维护命令

```bash
# SSH 登录服务器
ssh root@39.96.67.113

# 进入项目目录
cd /opt/jianfeidazi/backend

# 查看后端日志
pm2 logs jianfeidazi-backend

# 重启后端
pm2 restart jianfeidazi-backend

# 查看后端状态
pm2 status

# 拉取最新代码并重启
cd /opt/jianfeidazi && git pull origin main && pm2 restart jianfeidazi-backend

# 重启 Nginx
systemctl restart nginx

# 查看 Nginx 状态
systemctl status nginx
```

---

## 前端配置

已更新 `frontend/src/utils/request.js`：
- 生产环境（Android / iOS / 小程序真机）指向 `http://39.96.67.113/api`
- 开发环境（HBuilderX 预览 / H5 本地）指向 `http://localhost:3000/api`

---

## 后续待办

1. **修改服务器 root 密码**：当前密码曾在聊天中发送过，建议立即重置
2. **配置豆包 API Key**：编辑 `/opt/jianfeidazi/backend/.env`，填写真实的 AI 密钥
3. **购买域名 + 备案**（可选）：准备上架前再处理
4. **配置 HTTPS**（可选）：有域名后申请 SSL 证书
5. **上传 Android 调试包**：用 HBuilderX 打包 APK，连接真机测试

---

## 注意事项

- 当前后端使用 HTTP，正式上线前需切换为 HTTPS
- SQLite 数据库文件位于 `/opt/jianfeidazi/backend/data/app.db`，建议定期备份
- 调试证书 `android-certs/debug.keystore` 未提交到 GitHub，本地保留
